import { motion } from 'framer-motion';
import { Card, CardContent } from '@/components/ui/card';
import { MapPin, Clock, Users } from 'lucide-react';
import { services } from '@/lib/services';
import { Link } from 'react-router-dom';
import { Button } from '@/components/ui/button';
import OptimizedImage from '@/components/OptimizedImage';

export default function ServicesPage() {
  return (
    <div className="min-h-screen bg-[#FAF7F4]">
      {/* Hero Section */}
      <section className="relative min-h-[60vh] flex items-center justify-center overflow-hidden">
        <motion.div
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ duration: 1 }}
          className="absolute inset-0 bg-gradient-to-br from-[#2C1810]/90 via-[#5D3A1A]/80 to-[#7A4B2A]/85 backdrop-blur-sm"
        />
        <motion.div 
          initial={{ scale: 1.1 }}
          animate={{ scale: 1 }}
          transition={{ duration: 1.5 }}
          className="absolute inset-0 bg-[url('/images/services/hero-bg.jpg')] bg-cover bg-center bg-no-repeat bg-fixed"
        />
        
        <motion.div 
          initial={{ y: 30, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          transition={{ duration: 0.8, delay: 0.5 }}
          className="relative z-20 text-center max-w-[850px] mx-auto px-8"
        >
          <h1 className="text-6xl md:text-7xl font-bold text-white mb-6 tracking-tight drop-shadow-lg">
            Nuestros Servicios
          </h1>
          <p className="text-lg md:text-xl text-amber-100/90 mb-8">
            Descubre experiencias únicas en torno al café
          </p>
        </motion.div>
      </section>

      {/* Services Grid */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {services.map((service, index) => (
              <motion.div
                key={service.id}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                viewport={{ once: true }}
              >
                <Link to={`/servicios/${service.id}`}>
                  <Card className="group h-full hover:shadow-xl transition-shadow duration-300 overflow-hidden bg-white">
                    <div className="relative aspect-[4/3] overflow-hidden">
                      <OptimizedImage
                        src={service.image}
                        alt={service.title}
                        className="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
                      />
                      <div className="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                    </div>
                    <CardContent className="p-6">
                      <h3 className="text-2xl font-bold text-[#2A1810] mb-3 group-hover:text-[#734832] transition-colors">
                        {service.title}
                      </h3>
                      <p className="text-[#5D3A1A]/80 mb-4 line-clamp-2">
                        {service.description}
                      </p>
                      <div className="space-y-2 text-sm text-[#5D3A1A]/70">
                        <div className="flex items-center gap-2">
                          <MapPin className="w-4 h-4" />
                          <span>{service.location}</span>
                        </div>
                        <div className="flex items-center gap-2">
                          <Clock className="w-4 h-4" />
                          <span>{service.duration}</span>
                        </div>
                        <div className="flex items-center gap-2">
                          <Users className="w-4 h-4" />
                          <span>{service.capacity}</span>
                        </div>
                      </div>
                      <div className="mt-6">
                        <Button
                          className="w-full bg-[#734832] hover:bg-[#5D3A1A] text-white"
                          variant="default"
                        >
                          Ver más detalles
                        </Button>
                      </div>
                    </CardContent>
                  </Card>
                </Link>
              </motion.div>
            ))}
          </div>
        </div>
      </section>
    </div>
  );
}